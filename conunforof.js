function partir(arreglo, cantidad) { // tambien se puede usar metodo splice()
     const partes = [];
    // let parteActual = [];

    // for (const elemento of arreglo) {
    //     parteActual.push(elemento);

    //     if (parteActual.length === cantidad) {
    //         partes.push(parteActual);
    //         parteActual = [];
    //     }
    // }

    // if (parteActual.length > 0) {
    //     partes.push(parteActual);
    // }

    // return partes;
   
}

// Ejemplo de uso:
const resultado = partir([1, 2, 3, 4, 5], 2);
console.log(resultado);
